variables:
  GIT_SUBMODULE_STRATEGY: recursive

stages:
  - build

build: 
  stage: build
  script:
    - env
    - echo "HELLO WORLD!"
    - | 
      if [ -n ${TRIGGER_PAYLOAD} ]; then 
        cat ${TRIGGER_PAYLOAD}
      fi
